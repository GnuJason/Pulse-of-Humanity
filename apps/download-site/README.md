# Deprecated Download Site

This directory archives the former Flask/Render download site. It is not part of the screensaver build, packaging, test, or CI path.

The archived service consumes `shared/population.py` when launched from the repository root, but remains deprecated pending a separate decision to retain or replace the public download portal.